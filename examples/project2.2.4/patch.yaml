apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-app

spec:
  replicas: 5

  template:
    spec:
      containers:
      - name: nginx

        env:
        - name: ENVIRONMENT
          value: production

        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
