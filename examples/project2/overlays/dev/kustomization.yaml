resources:
- ../../base

namePrefix: perfix-
nameSuffix: -suffix

namespace: dev

labels:
  - pairs:
      environment: dev
    includeSelectors: true
    includeTemplates: true

annotations:
  - pairs:
      environment: dev
