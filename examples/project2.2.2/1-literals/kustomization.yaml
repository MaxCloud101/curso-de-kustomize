resources:
- deployment.yaml

secretGenerator:
- name: app-secret
  literals:
  - environment=prod
  - region=us-east-1

generatorOptions:
  disableNameSuffixHash: true
  labels:
    generated: "true"